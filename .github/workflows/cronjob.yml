name: Cron Job

on:
  # Triggers the workflow every 5 minutes
  # schedule:
  #   - cron: "*/1 * * * *"
  push:
    branches:
      - main

jobs:
  cron:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check-in
        uses: ./.github/workflows/check-in.yml
